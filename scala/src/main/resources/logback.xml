<configuration>
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd} %d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <root level="debug">
        <appender-ref ref="console"/>
    </root>

    <logger name="info.maaskant" level="debug"/>
    <logger name="org.pmw.tinylog" level="info"/> <!-- MFRC522 log messages -->
</configuration>
